http:
    # Lower http timeout to test that we return a 504 on timeouts
    timeout_ms: 1000

yelp_meteorite:
    etc_path: '/code/itest/data/etc'
    metrics-relay:
        host: '10.5.0.7'
        port: 1234

zipkin:
    syslog:
        host: '10.5.0.6'
        port: 514
