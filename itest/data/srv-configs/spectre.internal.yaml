cassandra:
    seeds_file: '/code/itest/data/synapse/services/cassandra_spectre.main.json'
    connect_timeout_ms: 500
    default_num_buckets: 1000
    keyspace: 'spectre_db'
    local_region: '.*'
    num_retries: 3
    read_timeout_ms: 100
    retry_on_timeout: false
    write_consistency: 'all'
    write_timeout_ms: 1000
